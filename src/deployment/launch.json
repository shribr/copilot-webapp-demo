{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open Deployment Directory",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/src/deployment",
            "console": "integratedTerminal"
        }
    ]
}